# Banco de dados PostgreSQL
# No Railway: adicione um plugin PostgreSQL e copie a DATABASE_URL gerada automaticamente
# No Replit: injetado automaticamente pelo ambiente
DATABASE_URL=postgresql://usuario:senha@host:5432/funeraria

# Porta do servidor (Railway injeta automaticamente — não precisa alterar)
PORT=5000
